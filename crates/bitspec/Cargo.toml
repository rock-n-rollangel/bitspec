[package]
name = "bitspec"
version = "0.1.0"
edition = "2024"

[features]
default = []
serde = ["dep:serde"]
transform = []

[dependencies]
serde = { version = "1", optional = true, features = ["derive"] }

[dev-dependencies]
criterion = "0.8.1"

[[bench]]
name = "schema_parse"
harness = false